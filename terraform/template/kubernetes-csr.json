{
  "CN": "kubernetes",
  "hosts": [
    "worker0",
    "worker1",
    "worker2",
    "${kubernetes_cluster_dns}",
    "${controller0_ip}",
    "${controller1_ip}",
    "${controller2_ip}",
    "${worker0_ip}",
    "${controller0_dns}",
    "${controller1_dns}",
    "${controller2_dns}",
    "${worker0_dns}",
    "${kubernetes_api_elb_dns_name}",
    "127.0.0.1"
  ],
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "JP",
      "L": "Tokyo",
      "O": "Minato",
      "OU": "Aoyama"
    }
  ]
}
